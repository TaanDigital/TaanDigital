---
const services = [
	{
		title: "Branding",
		description: "Creamos tu identidad visual y logotipo que refleje la esencia de tu negocio",
		icon: "🎨"
	},
	{
		title: "Marketing Digital",
		description: "Estrategias efectivas para llegar a tu audiencia ideal",
		icon: "📱"
	},
	{
		title: "Gestión de Contenido",
		description: "Mantenemos tus redes sociales activas y relevantes",
		icon: "📝"
	}
];
---

<section class="services">
	<h2>Nuestros Servicios</h2>
	<div class="services-grid">
		{services.map(service => (
			<div class="service-card">
				<div class="service-icon">{service.icon}</div>
				<h3>{service.title}</h3>
				<p>{service.description}</p>
			</div>
		))}
	</div>
</section>

<style>
	.services {
		padding: 4rem 2rem;
		background-color: white;
	}

	h2 {
		text-align: center;
		font-size: 2.5rem;
		color: #1a202c;
		margin-bottom: 3rem;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	.service-card {
		padding: 2rem;
		text-align: center;
		background-color: #f7fafc;
		border-radius: 1rem;
		transition: transform 0.3s ease;
	}

	.service-card:hover {
		transform: translateY(-5px);
	}

	.service-icon {
		font-size: 3rem;
		margin-bottom: 1rem;
	}

	h3 {
		color: #2d3748;
		font-size: 1.5rem;
		margin-bottom: 1rem;
	}

	p {
		color: #4a5568;
		line-height: 1.6;
	}
</style> 